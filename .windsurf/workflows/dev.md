---
description: Start the development server for the monorepo
---

1. Make sure dependencies are installed
// turbo
2. Run `pnpm dev` from the project root to start all apps in dev mode
// turbo
3. The web app will be available at http://localhost:3000
